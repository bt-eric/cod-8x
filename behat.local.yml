default:
  extensions:
    Behat\MinkExtension:
      # If necessary, change this to the actual URL of your COD site.
      base_url: 'http://127.0.0.1:8080'
    Drupal\DrupalExtension:
      drupal:
        # This assumes that we're running Behat from within
        # DRUPAL_ROOT/profiles/cod.
        drupal_root: '../../'